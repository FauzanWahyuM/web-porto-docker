{
    "type": "module",
    "name": "web-porto",
    "version": "1.0.0",
    "scripts": {
        "build": "rm -rf dist && mkdir -p dist/assets dist/components && cp index.html dist/ && cp -r assets/* dist/assets/ && cp -r components/* dist/components/"
    },
    "author": "Fauzan Wahyu M"
}